#!/usr/bin/env bash

# Only add to $PATH not already in $PATH
function path() {
	[[ ! -z "$1" ]] && [[ ":$PATH:" != *":$1:"* ]] && PATH="$PATH:$1"
}

path "/usr/local/sbin"
path "/Applications/Visual Studio Code.app/Contents/Resources/app/bin"
path "$HOME/.rvm/bin"
